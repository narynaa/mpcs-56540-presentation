## Set up
Download the required packages by running `pip3 install -r requirements.txt`.
Open a jupyter notebook session in whatever way you prefer (e.g. by running `jupyter notebook` in the terminal while this virtual environment is activated).

## Running the demo
Running the demo doesn't require any special instructions. Simply run each jupyter cell by clicking the run button or using the `Command + Enter` keyboard shortcut.